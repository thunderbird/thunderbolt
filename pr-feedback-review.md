# PR Feedback Review — THU-348 MCP Support

## Valid Findings

| # | PR | Author | Link | Comment | Why it makes sense | Suggested reply |
|---|-----|--------|------|---------|-------------------|-----------------|
| 1 | #500 | cursor | [r2977385850](https://github.com/thunderbird/thunderbolt/pull/500#discussion_r2977385850) | Dead `native_fetch` feature flag in Cargo.toml | The `native_fetch` feature in Cargo.toml does nothing now that the HTTP plugin is always registered. Dead code. | Good catch — we'll remove the feature flag since the HTTP plugin is now unconditionally registered. |
| 2 | #500 | raivieiraadriano92 | [r2994188978](https://github.com/thunderbird/thunderbolt/pull/500#discussion_r2994188978) | `encryptedCredential` sync discussion — suggests local-only table instead of column exclusion | He's right: PowerSync doesn't support local-only columns on synced tables (only local-only tables). Our `SELECT *` → explicit column list approach is a workaround. A proper local-only `mcp_credentials` table would be cleaner. | Great point Raí! You're right that PowerSync doesn't support local-only columns natively — only local-only tables. We should create a separate `mcp_credentials` local-only table instead of trying to exclude a column from sync rules. Let's sync on this. |
| 3 | #501 | cursor | [r2975809127](https://github.com/thunderbird/thunderbolt/pull/501#discussion_r2975809127) | Double `onclose` invocation in stdio transport | `close()` called `this.onclose?.()` after `child.kill()`, but `kill` also triggers the `cmd.on('close')` listener which fires `onclose` again. | Already fixed — removed the explicit `this.onclose?.()` from `close()`. |
| 4 | #501 | cursor | [r2976279550](https://github.com/thunderbird/thunderbolt/pull/501#discussion_r2976279550) | Missing double-start guard leaks child processes | `start()` has no guard — calling it twice overwrites `this.child` without killing the first process. Orphaned process. | Valid — we should add an early return if `this.child` is already set. Nice catch! |
| 5 | #501 | raivieiraadriano92 | [r2994408232](https://github.com/thunderbird/thunderbolt/pull/501#discussion_r2994408232) | Use discriminated union for `McpTransportConfig` | Suggests `{ type: 'http' \| 'sse'; url: string } \| { type: 'stdio'; command: string; args: string[] }` to avoid non-null assertions (`transport.url!`). | Love this suggestion Raí! A discriminated union makes impossible states unrepresentable and eliminates all the `!` assertions. We'll refactor `McpTransportConfig` in `src/types/mcp.ts`. |
| 6 | #502 | raivieiraadriano92 | [r2994463032](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2994463032) | Existing `getDeviceId` in `auth-token.ts` — potential overlap | There's already a `getDeviceId` in the codebase used by PowerSync. Asks if our MCP device ID needs to be per-installation or per-session. | Great question! Our MCP device ID is for encryption key derivation — it needs to be stable per installation (not reset on logout). The existing `getDeviceId` resets on logout which would invalidate all encrypted credentials. We should discuss whether to unify these or keep them separate. |
| 7 | #502 | cursor | [r2976914911](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2976914911) | Hardcoded `redirect_uris` duplicates Rust port list | The three OAuth redirect ports are hardcoded in both TypeScript and Rust without a shared constant. | Valid — if the Rust side ever changes the port range, the TS side would silently break. Low priority but worth a TODO. |
| 8 | #503 | raivieiraadriano92 | [r2994506618](https://github.com/thunderbird/thunderbolt/pull/503#discussion_r2994506618) | Export regex pattern for reuse in stdio transport | Suggests exporting the command validation regex from `mcp-utils.ts` to avoid duplication in `tauri-stdio-transport.ts`. | Already consolidated in the full branch — `tauri-stdio-transport.ts` imports validators from `mcp-utils.ts`. The duplication in stacked PRs is temporary (resolves on merge). Good eye though! |
| 9 | #503 | raivieiraadriano92 | [r2994533937](https://github.com/thunderbird/thunderbolt/pull/503#discussion_r2994533937) | `isValid` useCallback deps should be specific fields | Currently depends on entire `state`, so every state change invalidates memoization. Should use `[state.transportType, state.command, state.args, state.url]`. | Excellent catch! This is a real performance issue — every keystroke in any field invalidates the memoized `isValid`. We'll narrow the deps. |
| 10 | #503 | cursor | [r2977236500](https://github.com/thunderbird/thunderbolt/pull/503#discussion_r2977236500) | `SET_COMMAND` and `SET_ARGS` don't reset connection state | Changing command/args after a successful test should reset `connectionStatus` to `idle`, same as we do for `SET_URL`. | Valid — same logic as the URL reset we already fixed. Will add. |
| 11 | #504 | raivieiraadriano92 | [r2994586106](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2994586106) | Split `useMcpServersPageState` + use `useReducer` for the 5 `useState` hooks | The page state hook has 5 `useState` calls (violates CLAUDE.md 3+ threshold) and could be split into smaller hooks. | Good call — CLAUDE.md says useReducer for 3+ useState. We extracted the form state into useReducer already but the page-level state still has 5 useState. Will consolidate. |
| 12 | #504 | raivieiraadriano92 | [r2994608685](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2994608685) | Add eye icon toggle for password/bearer token field | UX improvement — let users toggle between password and text visibility. | Nice UX idea! We'll add a visibility toggle icon. Could also apply to the API key field in models settings as you mentioned. |
| 13 | #505 | raivieiraadriano92 | [r2994635926](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2994635926) | Use `McpTransportType` and `McpAuthType` from `src/types/mcp.ts` in DAL | The DAL uses inline string literal unions instead of importing the shared types. | Absolutely right — the types should come from the single source of truth. Will import from `@/types/mcp`. |
| 14 | #505 | raivieiraadriano92 | [r2994661162](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2994661162) | Remove unnecessary re-export of `MCPClient` type | `mcp-provider.tsx` re-exports `McpClient as MCPClient` but it's already exported from `@/types/mcp`. | Valid — dead re-export. Will remove. |
| 15 | #505 | raivieiraadriano92 | [r2994697763](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2994697763) | PowerSync config.yaml — `SELECT *` is fine, no new BE columns | Points out there are no new columns in the backend schema, so changing `SELECT *` to an explicit column list is unnecessary. Relates to the local-only column discussion in PR #500. | You're right Raí — since PowerSync syncs from the backend Postgres (not local SQLite), and the backend schema doesn't have these columns, `SELECT *` is safe. The explicit column list was premature. Will revert to `SELECT *`. |
| 16 | #505 | cursor | [r2977366673](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2977366673) | `updateMcpServerAuth` exported but never used | The function exists but nothing calls it. Dead code. | Valid — it was created for future OAuth token updates but nothing uses it yet. Will remove and add when needed. |
| 17 | #505 | cursor | [r2977366686](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2977366686) | Unguarded `JSON.parse` on `dbServer.args` | If `args` contains invalid JSON, `JSON.parse` throws and `syncServers` crashes. | Valid — we saw this bug during validation when args were saved as space-separated instead of JSON. A try/catch or validation would prevent silent sync failures. |
| 18 | #508 | cjroth | [r2977501055](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977501055) | Security implications of shell `args: true` | Allowing arbitrary arguments to `npx`, `node`, etc. expands attack surface. | Valid concern. The Tauri shell plugin enforces the allowlist at the Rust level so the webview can't call unlisted commands, but `args: true` means any arguments to those 5 commands. Application-level validation (`validateStdioCommand`/`validateStdioArgs`) provides a second layer. We should discuss whether regex-based arg validators in the capability are worth the rigidity trade-off. |
| 19 | #508 | cjroth | [r2977509318](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977509318) | `native_fetch` capability can be removed from capabilities list | Since the HTTP plugin is always registered, reporting it as a capability is misleading. | Valid — the capability response should always say `native_fetch: true` (which we already fixed) or we remove it entirely. |
| 20 | #508 | cjroth | [r2977510227](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977510227) | `tauri-plugin-shell = "2"` should be `"2.3"` | Other plugins use specific minor versions (e.g. `"2.3"`, `"2.5.4"`). Using just `"2"` allows any 2.x including old versions. | Good point — all other plugins pin to specific minor versions. We should pin to `"2.3"` or latest. |
| 21 | #508 | cjroth | [r2977521424](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977521424) | Mocking in tests — prefer dependency injection | Prefers DI over `mock.module` for credential-store and transport-factory tests. Acknowledges plugins are reasonable to mock. | Agree in principle. For Tauri plugins (external APIs not available in tests), mocking is the pragmatic choice per docs/testing.md. For the transport factory, DI is possible but changes the production API. Let's discuss the trade-off. |
| 22 | #508 | cjroth | [r2977525454](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977525454) | Sync with raivieiraadriano92 on credential-store vs e2ee overlap | Wants to align credential storage approach with the existing encryption-at-rest work. | Absolutely — we should make sure the MCP credential encryption aligns with the e2ee architecture. Will sync. |
| 23 | #508 | cjroth | [r2977530637](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977530637) | Use env var for CIMD domain (`thunderbolt.io`) | Self-hosted deployments may use a different domain. The CIMD client ID URL should be configurable. | Great point for open-source — hardcoding `thunderbolt.io` won't work for self-hosted instances. An env var or config setting makes sense. |
| 24 | #505 | cjroth | [ic4131013032](https://github.com/thunderbird/thunderbolt/pull/505#issuecomment-4131013032) | Remove legacy SSE, show stdio as disabled on web/mobile | Suggests removing SSE entirely and showing stdio in the dropdown everywhere but disabled with "(desktop app only)". | SSE is deprecated per MCP spec 2025-03-26 but some servers still only support it. Removing it would break compatibility with those servers. However, showing stdio everywhere (disabled on non-desktop) is a great UX idea — users discover the feature exists. |
| 25 | #505 | cjroth | [ic4131020446](https://github.com/thunderbird/thunderbolt/pull/505#issuecomment-4131020446) | How do we get OAuth working? | Asks about the "Connect with OAuth (coming soon)" button. | The `McpOAuthClientProvider` is implemented and the `createMcpOAuthProvider` factory is ready. The remaining work is wiring it into the transport factory when `authType === 'oauth'` and adding a UI flow to trigger it. This is planned as a follow-up since it requires testing against real OAuth MCP servers (GitHub, Notion). |
| 26 | #505 | cjroth | [ic4131044007](https://github.com/thunderbird/thunderbolt/pull/505#issuecomment-4131044007) | Render MCP connected but tools not working | Shows tools listed but they don't execute. Screenshot shows error. | Need to investigate — likely the tool call flow in `src/ai/fetch.ts` isn't properly forwarding the bearer token when executing tools (vs just listing them). The connection test passes because it only calls `tools()`. |
| 27 | #505 | cjroth | [ic4131059508](https://github.com/thunderbird/thunderbolt/pull/505#issuecomment-4131059508) | CORS error on web — suggests routing through backend proxy | Web version gets CORS errors for remote MCP servers. Suggests using the existing backend proxy pattern. | Valid architectural suggestion. The Tauri HTTP plugin bypasses CORS on desktop/mobile, but web has no such bypass. Routing MCP traffic through the Elysia backend (like other API calls) would solve this for the web version. |

## Invalid / Non-issue Findings

| # | PR | Author | Link | Comment | Why it doesn't make sense |
|---|-----|--------|------|---------|--------------------------|
| 1 | #500 | cursor | [r2975796526](https://github.com/thunderbird/thunderbolt/pull/500#discussion_r2975796526) | Version mismatch `0.1.68` vs `0.1.74` | Already fixed — we restored `package.json` and `Cargo.toml` from latest main. The bot saw an outdated diff. |
| 2 | #500 | cursor | [r2975796534](https://github.com/thunderbird/thunderbolt/pull/500#discussion_r2975796534) | HTTP plugin always registered but capability flag still gated | Already fixed — we updated `commands.rs` to always return `native_fetch: true`. |
| 3 | #500 | cursor | [r2975796540](https://github.com/thunderbird/thunderbolt/pull/500#discussion_r2975796540) | HTTP allowlist expanded to `https://**` | Intentional — MCP servers can be at any URL. We can't know them in advance. Ítalo already replied explaining this. |
| 4 | #501 | cursor | [r2975809134](https://github.com/thunderbird/thunderbolt/pull/501#discussion_r2975809134) | Duplicate `createTauriFetch` | The old `src/lib/tauri-http-transport.ts` is deleted in PR #505 (provider integration). The duplicate only exists temporarily across the stacked PRs. Non-issue once merged. |
| 5 | #502 | claude | [r2975733804](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2975733804) | `clientInfo` in-memory only, lost on restart | By design — CIMD static URL is the durable client identity. Dynamic client registration info is session-scoped per the MCP SDK's `OAuthClientProvider` interface. Persisting it would add complexity for no benefit. |
| 6 | #502 | claude | [r2976333915](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2976333915) | Prefer async/await over .then/.catch for keyPromise | The `??=` promise singleton pattern requires synchronous assignment of the not-yet-resolved promise. Rewriting as async/await would need an IIFE wrapper — same structure, no improvement. The `.then().catch()` is the idiomatic pattern here. |
| 7 | #502 | claude/cursor | [r2975733525](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2975733525) | Rejected promise caching in `keyPromise` | Already fixed — `.catch` reset pattern added. |
| 8 | #502 | claude/cursor | [r2975812558](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2975812558) / [r2975900831](https://github.com/thunderbird/thunderbolt/pull/502#discussion_r2975900831) | `tokens()` drops `scope` / hardcoded redirect port | Already fixed — `scope` added to return, `createMcpOAuthProvider` factory starts server and passes dynamic port. |
| 9 | #503 | cursor | [r2975793324](https://github.com/thunderbird/thunderbolt/pull/503#discussion_r2975793324) | Duplicated validation regex | Already consolidated in the full branch. Duplication in stacked PRs is inherent to stacking — resolves on merge. |
| 10 | #503 | cursor | [r2975793332](https://github.com/thunderbird/thunderbolt/pull/503#discussion_r2975793332) | `SET_TRANSPORT_TYPE` doesn't reset `serverCapabilities` | Already fixed — `serverCapabilities: []` added to the reducer case. |
| 11 | #504 | cursor/claude | [r2975815449](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975815449) / [r2977175113](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2977175113) | Auth config dropped / bearer token not saved | Already fixed — `authType` passed to mutation, `credentialStore.save()` called with bearer token. |
| 12 | #504 | cursor/claude | [r2975815457](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975815457) / [r2975853004](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975853004) | StatusIndicator invisible dot on error | Already fixed — maps `'error'` to `'offline'` (red dot). |
| 13 | #504 | cursor | [r2975815455](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975815455) | Space-separated args eats trailing spaces | Already fixed — `handleArgsInput` no longer filters while typing, only on submit. |
| 14 | #504 | cursor | [r2977098005](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2977098005) | Stale connection status after URL change | Already fixed — `SET_URL` and `SET_AUTH_TYPE` now reset `connectionStatus`. |
| 15 | #504 | cursor | [r2977098009](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2977098009) | Dialog dismiss triggers `closeAddDialog` twice | Already fixed — removed `onClose` prop, `onOpenChange` handles everything. |
| 16 | #504 | cursor | [r2975919979](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975919979) | Timeout timer leaks | Technically valid but harmless — `reject()` on an already-settled `Promise.race` is a no-op in all modern engines. Not worth the complexity of tracking the timer. |
| 17 | #504 | cursor | [r2975847642](https://github.com/thunderbird/thunderbolt/pull/504#discussion_r2975847642) | Status bar hides non-auth errors | Edge case with multiple servers failing for different reasons. Low impact. Can address as a follow-up UX improvement. |
| 18 | #505 | cursor | [r2975843558](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2975843558) | Unmount cleanup misses retry timeouts and transports | Already fixed — cleanup now clears all three refs. |
| 19 | #505 | cursor | [r2975843565](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2975843565) | Unreachable 60s delay | Already fixed — `maxAttempts = reconnectDelays.length`. |
| 20 | #505 | cursor | [r2975843573](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2975843573) | Duplicate `getMcpServers` | Already fixed — removed, kept `getAllMcpServers`. |
| 21 | #505 | cursor | [r2975843577](https://github.com/thunderbird/thunderbolt/pull/505#discussion_r2975843577) | Transport leaked on failed connection | Already fixed — `transportRefs.set()` now happens after `createMCPClient` succeeds. |
| 22 | #508 | cursor | [r2977345945](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977345945) | Test connection leaks client on timeout | Same as #16 — the timeout promise fires on an already-settled race. The `finally` block calls `mcpClient.close()`. Low risk. |
| 23 | #508 | cursor | [r2977345953](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977345953) | Broken re-exports of `validateCommand`/`validateArgs` | Only applies to the full branch where `tauri-stdio-transport.ts` was consolidated to import from `mcp-utils.ts`. The barrel export needs updating — but this is a full-branch-only issue, not in the stacked PRs. |
| 24 | #508 | cursor | [r2977345958](https://github.com/thunderbird/thunderbolt/pull/508#discussion_r2977345958) | Unused `createBearerAuthHeaders` | Exported but unused — correct. We removed `getEnvVarsForStdio` earlier for the same reason. Can remove this too, but it's a one-liner utility that may be used when OAuth token injection is wired. Low priority. |
