diff --git a/node_modules/@flwr/flwr/dist/flowerintelligence.bundled.es.js b/node_modules/@flwr/flwr/dist/flowerintelligence.bundled.es.js
index 8c86da3..675b537 100644
--- a/node_modules/@flwr/flwr/dist/flowerintelligence.bundled.es.js
+++ b/node_modules/@flwr/flwr/dist/flowerintelligence.bundled.es.js
@@ -641,7 +641,7 @@ class fL extends tF {
     aA(this, "baseUrl");
     aA(this, "apiKey");
     aA(this, "cryptoHandler");
-    this.baseUrl = pS, this.apiKey = I, this.cryptoHandler = new cL(this.baseUrl, this.apiKey);
+    this.baseUrl = yB.customBaseUrl || pS, this.apiKey = I, this.cryptoHandler = new cL(this.baseUrl, this.apiKey);
   }
   async chat(I, E, B, Q, C, D, M, R, y, i = !1, F) {
     if (i) {
@@ -54082,6 +54082,20 @@ const yB = class yB {
   set apiKey(g) {
     vD(yB, VD, g);
   }
+  /**
+   * Set base URL for FlowerIntelligence remote endpoint.
+   * @param baseUrl - The base URL to use instead of the default api.flower.ai
+   */
+  static set baseUrl(g) {
+    yB.customBaseUrl = g;
+  }
+  /**
+   * Get base URL for FlowerIntelligence remote endpoint.
+   * @returns The custom base URL if set, otherwise the default
+   */
+  static get baseUrl() {
+    return yB.customBaseUrl || pS;
+  }
   /**
    * Downloads and loads a model into memory.
    * @param model Model name of the model to download.
diff --git a/node_modules/@flwr/flwr/dist/flowerintelligence.es.js b/node_modules/@flwr/flwr/dist/flowerintelligence.es.js
index 145d434..b8e5626 100644
--- a/node_modules/@flwr/flwr/dist/flowerintelligence.es.js
+++ b/node_modules/@flwr/flwr/dist/flowerintelligence.es.js
@@ -620,7 +620,7 @@ class et extends X {
     g(this, "baseUrl");
     g(this, "apiKey");
     g(this, "cryptoHandler");
-    this.baseUrl = me, this.apiKey = t, this.cryptoHandler = new Oe(this.baseUrl, this.apiKey);
+    this.baseUrl = w.customBaseUrl || me, this.apiKey = t, this.cryptoHandler = new Oe(this.baseUrl, this.apiKey);
   }
   async chat(t, r, o, i, a, s, l, y, f, u = !1, p) {
     if (u) {
@@ -1171,6 +1171,20 @@ const w = class w {
   set apiKey(n) {
     P(w, S, n);
   }
+  /**
+   * Set base URL for FlowerIntelligence remote endpoint.
+   * @param baseUrl - The base URL to use instead of the default api.flower.ai
+   */
+  static set baseUrl(n) {
+    w.customBaseUrl = n;
+  }
+  /**
+   * Get base URL for FlowerIntelligence remote endpoint.
+   * @returns The custom base URL if set, otherwise the default
+   */
+  static get baseUrl() {
+    return w.customBaseUrl || me;
+  }
   /**
    * Downloads and loads a model into memory.
    * @param model Model name of the model to download.
