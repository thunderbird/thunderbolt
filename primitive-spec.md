 I want to redesign the app so that agents are the compartmentalizaiton tool - not workspaces. Workspaces are changed to projects (which are basically folders) or can be removed. Most primitives live inside of agents now. External ACP agents will have their own mcp/integrations/skills. The current skills/agents/mcps don't need to be nested under workspaces - instead they are just user-scoped and get injected into every agent (as part of the prompt). Users can create multiple thunderbolt (built-in) agents, not just one. So when they got add an agent, there will be a new option to add a Thunderbolt agent. So there's basically agents as compartmentalization, which each one has its own mcps/skills/connectors/ect. External ACP agents have those things internaly anyways, and we can't really see them - that's fine, it's just opaque to the app. Internal Thunderbolt agents though we do manage and control in our local db and can edit. So where we see these configs in the settings now, we'd have a way to access the same settings pages, but PER AGENT. This could be accordion menus in the sidebar per agent (sounds kinda ugly) or maybe a dropdown in settings pages to tell it which agent we're currently looking at. I don't love that paradigm either. I want you to really think through ideas here for how we could design this the best possible way - don't get too biased by what I'm suggesting - you have full creative freedom. In the future, we'll control which users have access to which agents by creating a separate closed source admin app / control plane that is separate from thunderbolt where admins can grant access to agents for paritcular users or groups of users. This will integrate with IDPs - eg, if IDP defines groups, we can reuse those groups. For now, we could just build that screen into the admin settings if it makes sense. In addition to the per-agent settings, the prupose of the user-scoped settings is to let users define their own skills/connectors/etc that get added on top of whatever the agent has. Providers would exist outside of agents (user scoped) and be used for standalone mode so that the user can easily add search / models without having to reconect api keys / accounts every time.

  I want you to interivew me and help me fund the unknown unknowns and fill in my blind spots to turn this into a new product spec.
